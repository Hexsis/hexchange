type PingResponse {
    result: String @cache(maxAge: 30, policy: PRIVATE)
    health: String @cache(maxAge: 30)
}

type Query {
    ping: PingResponse @hasRole(anyOf: ["customer"])
}

schema {
    query: Query
}